<?xml version="1.0" encoding="utf-8"?>
<form>
    <fields name="attribs">
        <fieldset name="basic">

            <field type="spacer"
                   hr="true"
            />

            <field name="customcss"
                   type="editor"
                   editor="codemirror"
                   label="Styles personnalisés"
                   filter="raw"
            />

            <field name="customjs"
                   type="editor"
                   editor="codemirror"
                   label="JS personnalisés"
                   filter="raw"
                    />

        </fieldset>
    </fields>
</form>